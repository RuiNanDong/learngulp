# `learngulp`

## `gulp`和相关插件安装

* `npm install -g gulp`    全局安装`gulp`
* `npm install gulp --save-dev` 项目下安装`gulp`
* `npm install gulp-less --save-dev`  编译`less`文件插件
* `npm install gulp-connect --save-dev` 运行本地服务器插件